RABBITMQ_USER=guest
RABBITMQ_PASS=guest
# For local testing: host.docker.internal lets containers reach the host machine
# Replace with actual Azure VM1 private IP when deploying (e.g. 10.0.0.4)
VM1_PRIVATE_IP=localhost
RABBITMQ_IP=host.docker.internal
KONG_IP=20.100.192.182
