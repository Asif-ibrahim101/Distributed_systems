# For local testing: host.docker.internal lets containers reach the host machine
# Replace with actual Azure VM2 private IP when deploying (e.g. 10.0.0.5)
DB_TYPE=mongo
DB_HOST=mongodb
DB_ROOT_PASSWORD=rootpassword
DB_NAME=jokedb
DB_USER=jokeuser
DB_PASSWORD=jokepassword
RABBITMQ_USER=guest
RABBITMQ_PASS=guest
RABBITMQ_IP=10.0.0.5
